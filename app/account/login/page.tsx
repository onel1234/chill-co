import LoginClient from '@/components/LoginClient';

export const metadata = {
  title: 'Sign In | Chill Co.',
  description: 'Sign in to your Chill Co. account.',
};

export default function LoginPage() {
  return <LoginClient />;
}
