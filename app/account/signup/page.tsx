import SignupClient from '@/components/SignupClient';

export const metadata = {
  title: 'Create Account | Chill Co.',
  description: 'Join Chill Co. and start shopping.',
};

export default function SignupPage() {
  return <SignupClient />;
}
